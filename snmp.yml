logging_global:
    hosts:
    -   host: syslog1.shadowman.dev
    -   host: syslog2.shadowman.dev
snmp_server:
    communities:
    -   name: new_read_write
        rw: True
    -   name: new_read_only
        ro: True
